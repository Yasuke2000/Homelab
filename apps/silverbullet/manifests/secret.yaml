---
# ---------------------------------------------------------------------------
# SilverBullet — Kubernetes Secret
# Encrypt before committing: sops apps/silverbullet/manifests/secret.yaml
# ---------------------------------------------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: silverbullet-secrets
  namespace: silverbullet
type: Opaque
stringData:
  # Generate: openssl rand -base64 32
  password: REPLACE_WITH_PASSWORD
