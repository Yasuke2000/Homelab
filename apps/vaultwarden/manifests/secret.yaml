---
# ---------------------------------------------------------------------------
# Vaultwarden — Kubernetes Secret
# Encrypt before committing: sops apps/vaultwarden/manifests/secret.yaml
# ---------------------------------------------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: vaultwarden-secrets
  namespace: vaultwarden
type: Opaque
stringData:
  # Generate: openssl rand -base64 48
  adminToken: REPLACE_WITH_ADMIN_TOKEN
