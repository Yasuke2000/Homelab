---
# ---------------------------------------------------------------------------
# RomM — Kubernetes Secret
# Encrypt before committing: sops apps/romm/manifests/secret.yaml
# ---------------------------------------------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: romm-secrets
  namespace: romm
type: Opaque
stringData:
  dbPassword: REPLACE_WITH_DB_PASSWORD
  # Generate: openssl rand -hex 32
  secretKey: REPLACE_WITH_SECRET_KEY
