---
# ---------------------------------------------------------------------------
# Pelican — Kubernetes Secret
# Encrypt before committing: sops apps/pelican/manifests/secret.yaml
# ---------------------------------------------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: pelican-secrets
  namespace: pelican
type: Opaque
stringData:
  dbPassword: REPLACE_WITH_DB_PASSWORD
