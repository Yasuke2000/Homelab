---
# ---------------------------------------------------------------------------
# Shelf — Kubernetes Secret
# Encrypt before committing: sops apps/shelf/manifests/secret.yaml
# ---------------------------------------------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: shelf-secrets
  namespace: shelf
type: Opaque
stringData:
  # Generate: openssl rand -base64 32
  sessionSecret: REPLACE_WITH_SESSION_SECRET
