---
# ---------------------------------------------------------------------------
# Ghost — Kubernetes Secret
# Encrypt before committing: sops apps/ghost/manifests/secret.yaml
# ---------------------------------------------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: ghost-secrets
  namespace: ghost
type: Opaque
stringData:
  dbPassword: REPLACE_WITH_DB_PASSWORD
