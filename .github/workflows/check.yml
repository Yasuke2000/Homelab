---
# ---------------------------------------------------------------------------
# GitHub Actions — CI for NixOS homelab repo
#
# Runs on every push and PR:
#   1. nix flake check — validates all NixOS configurations
#   2. yamllint — validates all YAML manifests
#   3. kubeconform — validates K8s manifests against API schemas
#   4. sops check — ensures no unencrypted secrets are committed
#
# Uses Cachix to speed up Nix builds (optional, TODO to configure)
# ---------------------------------------------------------------------------

name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  nix-flake-check:
    name: Nix flake check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v30
        with:
          nix_path: nixpkgs=channel:nixos-25.05
          extra_nix_config: |
            experimental-features = nix-command flakes
            accept-flake-config = true

      # TODO: configure Cachix for faster CI builds
      # - name: Use Cachix cache
      #   uses: cachix/cachix-action@v15
      #   with:
      #     name: YOUR_CACHIX_CACHE_NAME
      #     authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}

      - name: Check flake
        run: nix flake check --no-build --show-trace

      - name: Show flake metadata
        run: nix flake metadata

  yaml-lint:
    name: YAML lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run yamllint
        uses: ibiqlik/action-yamllint@v3
        with:
          config_file: .yamllint.yaml

  kubeconform:
    name: Kubernetes manifest validation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install kubeconform
        run: |
          curl -sL https://github.com/yannh/kubeconform/releases/latest/download/kubeconform-linux-amd64.tar.gz \
            | tar -xz -C /usr/local/bin kubeconform

      - name: Validate K8s manifests
        run: |
          find apps infrastructure -name '*.yaml' -o -name '*.yml' | \
          grep -v 'application.yaml' | \
          xargs kubeconform \
            -strict \
            -ignore-missing-schemas \
            -kubernetes-version 1.33.0 \
            -summary

  sops-check:
    name: No plaintext secrets
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check secrets are encrypted
        run: |
          # Fail if secrets.yaml exists, has real values, but is not sops-encrypted.
          # A file with only REPLACE_WITH_ placeholders is a template — skip it.
          if [ -f secrets/secrets.yaml ]; then
            has_sops=$(grep -c "^sops:" secrets/secrets.yaml 2>/dev/null || true)
            has_enc=$(grep -c "ENC\[AES256_GCM" secrets/secrets.yaml 2>/dev/null || true)
            # If it looks like it has real values (not just REPLACE_WITH_ placeholders)
            has_real=$(grep -Ev "(REPLACE_WITH_|^#|^---$|^\s*$)" secrets/secrets.yaml | grep -v "^sops:" | wc -l)
            if [ "$has_sops" -eq 0 ] && [ "$has_enc" -eq 0 ] && [ "$has_real" -gt 0 ]; then
              echo "ERROR: secrets/secrets.yaml appears to contain unencrypted values!"
              echo "Run: sops secrets/secrets.yaml  to encrypt it first."
              exit 1
            fi
          fi
          echo "Secrets check passed"

      - name: Scan for accidental secrets
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: ${{ github.event.repository.default_branch }}
          head: HEAD
          extra_args: --only-verified

  line-endings:
    name: Check LF line endings
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check for CRLF in Nix files
        run: |
          crlf_files=$(find . -name '*.nix' -exec file {} \; | grep CRLF | awk -F: '{print $1}')
          if [ -n "$crlf_files" ]; then
            echo "ERROR: CRLF line endings found in .nix files:"
            echo "$crlf_files"
            echo "Fix with: git add --renormalize ."
            exit 1
          fi
          echo "Line ending check passed"
